---
name: Filterable Input
---

import { Playground, Props } from 'docz'
import FilterableInput from './filterable-input'



# Filterable Input

## Basic usage

<Playground>
  <FilterableInput
    name='filter'
    initialFilters={[{
      key: 'status',
      value: 'online'
    }]}
    availableFilters={[{
      key: 'status',
      values: ['online', 'offline', 'unknown']
    }, {
      key: 'type',
      values: ['lamp', 'alarm', 'other']
    }]}
  />
</Playground>

## Properties

<Props of={FilterableInput} />
