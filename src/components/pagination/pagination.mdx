---
name: Pagination
---

import { Playground, PropsTable } from 'docz'
import Pagination from './pagination'
import '../../global.jsx'

# Pagination

## Basic usage

<Playground>
  <Pagination count={9} onIndexChanged={index => window.alert(index)} />
</Playground>

<Playground>
  <Pagination count={10} />
</Playground>

<Playground>
  <div style={{ margin: '15px 0' }}>
    <Pagination count={100} />
  </div>
  <div style={{ margin: '15px 0' }}>
    <Pagination count={100} activeIndex={2} />
  </div>
  <div style={{ margin: '15px 0' }}>
    <Pagination count={100} activeIndex={50} />
  </div>
  <div style={{ margin: '15px 0' }}>
    <Pagination count={100} activeIndex={97} />
  </div>
  <div style={{ margin: '15px 0' }}>
    <Pagination count={100} activeIndex={99} />
  </div>
</Playground>

<Playground>
  <Pagination />
</Playground>

## Properties

<PropsTable of={Pagination}></PropsTable>
